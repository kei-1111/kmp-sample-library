name: iOS Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build and Package XCFramework
        run: |
          ./gradlew :shared:packageXCFramework

      - name: Read checksum
        id: checksum
        run: |
          SHARED_CHECKSUM=$(cat shared/build/outputs/checksum.txt)
          echo "shared_checksum=$SHARED_CHECKSUM" >> $GITHUB_OUTPUT
          echo "📋 Shared Checksum: $SHARED_CHECKSUM"

      - name: Update Package.swift with checksum
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          SHARED_CHECKSUM=$(cat shared/build/outputs/checksum.txt)

          # Package.swiftのバージョンとチェックサムを更新
          sed -i.bak "s|/releases/download/v[0-9.]*|/releases/download/v${VERSION}|g" Package.swift
          sed -i.bak "s|checksum: \".*\"|checksum: \"${SHARED_CHECKSUM}\"|" Package.swift
          rm -f Package.swift.bak

          echo "✅ Package.swift を更新しました"

      - name: Commit and push Package.swift
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add Package.swift

          # Package.swiftに変更があればコミット
          if ! git diff --staged --quiet; then
            git commit -m "chore: Package.swiftを${{ github.ref_name }}のチェックサムで更新"

            # タグを新しいコミットに移動
            git tag -f ${{ github.ref_name }}

            # プッシュ（タグも強制更新）
            git push origin HEAD:main
            git push -f origin ${{ github.ref_name }}

            echo "✅ Package.swiftをコミットしてタグを更新しました"
          else
            echo "ℹ️ Package.swiftに変更がありませんでした"
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            shared/build/outputs/Shared.xcframework.zip
          body: |
            ## KMP Sample Library Release

            ### Swift Package Manager

            Xcodeでこのライブラリを使用するには、以下の手順で追加してください：

            1. Xcode → File → Add Package Dependencies
            2. リポジトリURL: `https://github.com/kei-1111/kmp-sample-library`
            3. バージョン: `${{ github.ref_name }}`を選択
            4. Sharedライブラリを選択

            ### XCFramework Checksum

            **Shared:**
            ```
            ${{ steps.checksum.outputs.shared_checksum }}
            ```

            Package.swiftファイルを手動で更新する場合は、上記のchecksumを使用してください。
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}